# Copy to .env and fill in. .env is gitignored — never commit it.

# Secret for the default LLM provider (Anthropic / Claude).
# Only the key for the provider selected in config.yaml (llm.provider) is needed.
ANTHROPIC_API_KEY=

# Future providers (only needed if you switch llm.provider in config.yaml):
# OPENAI_API_KEY=
