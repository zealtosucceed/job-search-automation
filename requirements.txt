# Core
PyYAML>=6.0
python-dotenv>=1.0

# LLM (adapter registry makes the provider swappable — see config.yaml llm.provider)
anthropic>=0.49
openai>=1.40
groq>=0.9

# Resume parsing (PDF input)
PyMuPDF>=1.24

# Document generation (DOCX + Markdown only — no PDF deps)
python-docx>=1.1

# Collectors
requests>=2.31
beautifulsoup4>=4.12
playwright>=1.44          # LinkedIn Mode C (run: playwright install chromium)

# Scheduling
APScheduler>=3.10
tzdata>=2024.1            # IANA tz database (Australia/Sydney) on minimal systems

# Dashboard
streamlit>=1.36
pandas>=2.0               # dashboard tables / CSV export
